#include <raylib.h>
#include "inventory.h"
inventory::inventory(int winWidth, int winHeight)
{
    width = stashTab.width;
    height = stashTab.height;
    //Function to store a list of integers in a file
//     void storeListToFile(const std::list<int>& inventory_list, const std::string& filename) 
//     {
//         std::vector<std::string> stringList = {"apple", "banana", "cherry"};
//         std::ofstream outputFile(filename);
//         if (outputFile.is_open()) {
//             for (const std::string& str : stringList) {
//                 outputFile << str << std::endl;
//             }
//             outputFile.close();
//         }
//     }
//     // Function to read a list of integers from a file
//     std::list<> readListFromFile(const std::string& filename){}        
};
